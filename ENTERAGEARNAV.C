#include <stdio.h> 
int main() { 
    int age; 
    // asking user for input
    printf("enter your age:");
    scanf("%d", &age);
    // printing the age
    printf("your age is %d\n",age);
    return 0;
}